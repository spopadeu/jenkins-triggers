pipeline {
	agent any
	stages {
		stage('Build') {
			steps{
				sh 'echo "Build completed."'
			}
		}

		stage('Test Trigger') {
        			steps{
        				sh 'echo "Trigger works."'
        			}
        		}
	}
}